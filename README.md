#Image Data Extract & Compare (Streamlit App)

## Features
- Upload image
- Extract text using OCR (pytesseract)
- Detect temperature number
- Compare with OpenWeather API

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
